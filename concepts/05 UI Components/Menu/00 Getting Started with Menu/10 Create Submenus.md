To create a submenu, use nested [items](). In jQuery, it's an array inside the base item object. In Angular, Vue and React, it's another item inside the base item markup.

Use the [beginGroup]() property to separate items in the submenu.