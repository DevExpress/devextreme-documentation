[note] For simplicity, this tutorial displays a Toast in response to a click on a [Button](/api-reference/10%20UI%20Components/dxButton '/Documentation/ApiReference/UI_Components/dxButton/'). Your application's logic will likely be different, but you can still re-use the code. 

