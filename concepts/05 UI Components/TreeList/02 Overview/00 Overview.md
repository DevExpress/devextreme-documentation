DevExtreme TreeList is a UI component for displaying hierarchical data in a grid. It can handle a collection of linked plain objects and still build a tree hierarchy. Key features include data editing and validation, sorting, filtering, searching, and adaptability.

In this overview, you can find a map of TreeList elements, information about key features, and what to explore next.