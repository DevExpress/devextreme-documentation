The Form component displays fields from data objects as a collection of label-editor pairs. You can process the form data, validate it, and send to a server.

This tutorial shows how to add the Form to a page, create form items, adjust them, validate items data and submit the form. As a result, you will get a widget that looks as follows: