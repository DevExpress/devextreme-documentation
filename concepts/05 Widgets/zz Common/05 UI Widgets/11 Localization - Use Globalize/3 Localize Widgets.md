Use Globalize methods to localize date, number and currency values and messages.

    var buttonOptions = {
        text: Globalize.formatMessage("Done");
        . . .
    }

For the information on Globalize localization API, refer to the [Globalize documentation](https://github.com/globalizejs/globalize).