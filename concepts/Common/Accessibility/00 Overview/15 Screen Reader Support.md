Screen reader support allows users with visual impairments to access and comprehend the content. DevExtreme components include the following features that ensure proper screen reader work:

- `aria-*` labels.
- Descriptions.
- Focus management.