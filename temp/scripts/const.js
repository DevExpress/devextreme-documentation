const ARG_ARTIFACTS = "artifacts";
const ARG_CONTENT_MAP_PATH = "content-map-path";
const ARG_DECLARATIONS_PATH = "declarations-path";
const ARG_DOCS_ROOT = "docs-root";
const ARG_EMUNS_JSON_PATH = "enum-json-path";
const ARG_FAIL_ON_CHANGE = "fail-on-change"
const ARG_JS_SCRIPTS = "js-scripts";
const ARG_LIST_FILES = "list-file";
const ARG_OUTPUT = "output";
const ARG_OUTPUT_PATH = "output-path";
const ARG_SEARCH_PATERN = "search-pattern";
const ARG_TARGET_PATH = "target-path";
const ARG_VALIDATION = "validation";

module.exports = {
    artifacts: ARG_ARTIFACTS,
    contentMapPath: ARG_CONTENT_MAP_PATH,
    declarationsPath: ARG_DECLARATIONS_PATH,
    docsRoot: ARG_DOCS_ROOT,
    enumsJsonPath: ARG_EMUNS_JSON_PATH,
    failOnChange: ARG_FAIL_ON_CHANGE,
    jsScripts: ARG_JS_SCRIPTS,
    listFiles: ARG_LIST_FILES,
    output: ARG_OUTPUT,
    outputPath: ARG_OUTPUT_PATH,
    searchPattern: ARG_SEARCH_PATERN,
    targetPath: ARG_TARGET_PATH,
    validation: ARG_VALIDATION
}